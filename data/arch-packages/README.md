# arch-packages

Custom metapackages for Arch Linux that include packages I like to have
installed.

(Metapackages are conventionally just empty packages that depend on other
packages, so that installing the metapackage installs all the dependencies.)
