# yaml-language-server: $schema=./.pacman-packagelist.schema.json

# Installed on machines connected to my Brother printer, a Brother MFC-7860DW.
# Gated on the usesBrotherPrinter config variable.
#
# Placement rule: drivers/packages tied to this specific peripheral. Like the
# thinkpad list, peripheral-specific packages get their own list.

- "brlaser" # Brother printer drivers
