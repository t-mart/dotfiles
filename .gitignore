# gitignore file for this dotfiles repository.
#
# This is *not* the deployed gitignore file. That is `git/ignore`.

# this leaks files names of recently played media and isn't really important to
# persist
mpv.net/config/settings.xml
